from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A small emotion detection project to find emotion from the given text',
    author='abhishek',
    license='',
)
